<% if obj.errors.any? %>
  <p><%= pluralize(obj.errors.count,"error") %> prevented the article from being created</p>
	<ul>
		<% obj.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
	</ul>
<% end %>
